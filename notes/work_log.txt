Work log: Bayesian-ECD Shiny App organization

1. Created a clean repository structure for the minimum runnable Shiny app before future conversion into a formal R package.
2. Preserved untouched source files under original_files/:
    * usingGowerDistance.R
    * Robust_stage_1and2.cpp
    * helper_functons.R
    * app.R
    * helpers.R
    * custom.css
3. Split the app code into:
    * app/: Shiny application files
    * app_core/: core modeling, plotting, RMST, subgroup, survival, and C++ routines
    * app/data/storedMCMCiter/: stored baseline MCMC components
    * analysis_utils/: optional user-facing posterior probability utilities
    * examples/: example scripts
    * example_data/: example model-fitting and prediction datasets
    * notes/: development notes and dependency map
4. Cleaned and sourced the app-core files:
    * Robust_stage_1and2.cpp
    * survival-utils.R
    * cappmx-extend-approx-fit.R
    * plotting.R
    * rmst-utils.R
    * subgroup-analysis.R
5. Confirmed that the cleaned app-core source test passes and that all required app-core functions exist.
6. Updated app.R to source local app_core files instead of relying on an installed ExtendedCAPPMx package.
7. Split the stored baseline MCMC object into component-level .rds files under app/data/storedMCMCiter/ to avoid tracking one large GitHub object.
8. Updated helpers.R to load the stored MCMC components from app/data/storedMCMCiter/ and reconstruct storedMCMCiter for app use.
9. Replaced ExtendedCAPPMx-dependent parallel-worker behavior with foreach/doParallel workers using locally sourced functions passed through .export.
10. Replaced R-level use of the Rcpp log_sum_exp() helper with matrixStats::logSumExp() inside survival/density helpers to avoid external-pointer issues in PSOCK workers.
11. Added prediction support for covariate-only datasets, including:
    * Prediction Data tab
    * Prediction Output tab
    * Prediction RMST Output tab
12. Implemented pred_result() using cappmx_extend_approx_fit(…, input_df_pred = pred_data()) to generate Predicted_Allocation_variables.
13. Reused subgroup_data(…, use_pred = TRUE) for full prediction-dataset and selected prediction-subgroup survival and hazard-ratio summaries.
14. Reused RMST_result() for prediction RMST summaries, making prediction RMST output structurally parallel to the existing subgroup RMST output.
15. Confirmed that the prediction workflow runs successfully in the Shiny app.
16. Added optional posterior probability utilities under analysis_utils/:
    * posterior_prob_hr_lt()
    * posterior_prob_rmst()
17. Added examples/run_posterior_probabilities.R to demonstrate use of the posterior probability utilities with the included example datasets.
18. Added DESCRIPTION and README.md files documenting the app metadata, dependencies, installation steps, app launch instructions, example workflow, prediction workflow, and optional posterior probability utilities.
19. Moved custom.css into app/www/custom.css so Shiny serves the stylesheet correctly.
20. Updated app.R to reference custom.css from the Shiny www directory.
21. Updated source_app_core.R to locate the repository root more robustly and source app_core files using absolute paths.
22. Added dependency checks to source_app_core.R so missing packages are reported before compiling or sourcing app-core routines.
23. Added mcclust to the dependency list after identifying it as required for app execution.
24. Updated app.R data-loading logic so categorical variables are converted to factors before model, subgroup, and prediction analyses.
25. Updated prediction-data loading so categorical variables and prediction IDs are handled consistently with model-fitting data.
26. Added Shiny session cleanup using session$onSessionEnded() to reset foreach to sequential mode, run garbage collection, and stop the local app when the session ends.
27. Updated subgroup-analysis.R so parallel workers are cleaned up with on.exit() immediately after cluster creation.
28. Replaced final subgroup-analysis.R cluster cleanup with safe try(parallel::stopCluster(…)) and foreach::registerDoSEQ() calls.
29. Updated README.md to recommend RStudio Run App > Run in Window as the preferred local launch method.
30. Documented that external browser mode may open multiple tabs if older Shiny sessions remain active or if RStudio/browser launcher state is not cleared.
31. Removed the RcppGSL dependency from Robust_stage_1and2.cpp, DESCRIPTION, README.md, and source_app_core.R.
32. Replaced former GSL special-function calls with R/Rcpp-compatible alternatives:
    * R::digamma()
    * R::lgammafn()
33. Replaced former GSL random-number utilities with local helper functions using:
    * R::runif()
    * R::rgamma()
34. Replaced former GSL utility macros/functions such as gsl_pow_2, GSL_MAX_INT, and GSL_MIN_DBL with local C++ helpers.
35. Removed unused OpenMP support from the C++ source by deleting the omp.h include and the Rcpp OpenMP plugin line.
36. Confirmed that the retained C++ code no longer requires external GSL or OpenMP system support.
37. Updated app_core_dependency_map.txt to reflect:
    * reduced dependency set
    * removal of RcppGSL/GSL/OpenMP requirements
    * RcppArmadillo/RcppDist C++ requirements
    * parallel-worker cleanup expectations
    * recommended Shiny launch method
38. Updated README.md clone instructions to use the actual GitHub repository path.
39. Confirmed the repository is organized as a minimum runnable Shiny app before future conversion into a formal R package.